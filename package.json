{
  "name": "cicd-demo",
  "version": "1.0.0",
  "description": "Simple Node.js app with end-to-end CI/CD pipeline via GitHub Actions",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "test": "node --experimental-vm-modules node_modules/.bin/jest --coverage",
    "lint": "eslint . --ext .js"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "eslint": "^8.56.0",
    "jest": "^29.7.0",
    "supertest": "^6.3.4"
  },
  "jest": {
    "testEnvironment": "node"
  }
}
